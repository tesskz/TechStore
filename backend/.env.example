# Copie ce fichier en ".env" et remplis tes valeurs
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=techstore
JWT_SECRET=
PORT=3000

# Stripe (https://dashboard.stripe.com/test/apikeys) — utilise les clés "test" en dev
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
